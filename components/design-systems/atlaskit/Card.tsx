'use client';

// Placeholder for AtlasKit card since specific card package isn't in package.json
// Normally we'd use @atlaskit/smart-card but we'll build a simple one

export default function Card() {
  return (
    <div className="text-sm text-muted-foreground italic border border-dashed border-border p-2 rounded text-center">
      Card not available in Atlaskit
    </div>
  );
}
